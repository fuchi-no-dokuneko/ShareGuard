<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <application>
        <!-- Test-APK component: a distinct package/UID used only to verify scoped URI grants. -->
        <activity
            android:name="app.shareguard.canonical.TestShareReceiverActivity"
            android:exported="true"
            android:theme="@android:style/Theme.Material.Light.NoActionBar" />
    </application>
</manifest>
