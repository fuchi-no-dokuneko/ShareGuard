acceptance/harness.py: Custom Gherkin parsing, unique binding validation, ADB orchestration, recording hooks, screenshots, checklist, and Sonar XML are tested by dry execution; Android product modules are not mocked.
acceptance/features: Home, text, image, review, workflow, verification, saved-result, settings, share, export, deletion, persistence, and privacy modules are specified; real Android execution requires an authorized device.
core/security: In-memory keys are tested; Android Keystore nonce generation is exercised by release instrumentation, not mocked.
Android test modules: AndroidJUnitRunner packaging is validated by test-APK assembly; device behavior is exercised in the API 23 and API 36 release lanes.
app: Test receiver compatibility and mutable/immutable bitmap cleanup are exercised by connected Android release tests; platform behavior is not mocked.
app/androidTest: Standalone receiver runtime, persisted fixture cleanup, and deletion UI synchronization are tested on API 23 and API 36; no app services are mocked.
app/androidTest sharing: Cross-package URI grants use a platform result callback and connected tests; neither provider permissions nor receiver execution are mocked.
feature/saved accessibility: Saved-result cards expose focus semantics and connected Compose tests verify actual focus; UI focus handling is not mocked.
app/androidTest intent lifecycle: Sanitized share intents use direct platform launches while default lifecycle uses ActivityScenario; Android lifecycle and intent handling are not mocked.
CI emulator matrix: API 23 uses the upstream-tested 32-bit legacy runner profile; API 36 retains the modern profile. Both execute the same unmocked instrumentation suite.
block/text API 23: UTF-16 scalar traversal is unit-tested with a supplementary compatibility letter; API 23 instrumentation exercises the same implementation without mocks.
